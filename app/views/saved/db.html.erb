<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @saved_items.each do |s| %>
      <tr>
        <td><%= s.item.name %></td>
        <td><%= s.user.id %></td>
      </tr>
    <% end %>
  </tbody>
</table>
